# Pinned dependency versions for reproducibility.
# Install with: pip install -r requirements.lock

# Core scientific stack (used by the main pipeline + verify_results)
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.4.2
xgboost==2.0.3
shap==0.44.1
matplotlib==3.8.4
scipy==1.12.0
PyYAML==6.0.2

# Per-fold ComBat batch correction (scripts/batch_correction.py only)
combat==0.3.3

# Adenoma class-imbalance robustness sweep (SMOTE + random undersampling
# in scripts/rebalanced_adenoma_lodo.py)
imbalanced-learn==0.12.3

# Manuscript docx build (manuscript/markdown/_build_docx.py)
python-docx==1.1.2

# Test runner (tests/, scripts/test_submission_build.py)
pytest==8.2.2

# Optional submission-build dependencies (scripts/build_submission.py
# falls back gracefully if these are missing; install only if you intend
# to regenerate submission/build/SUBMISSION_BUNDLE.zip with merged PDFs):
# pypdf==4.2.0
# img2pdf==0.5.1
