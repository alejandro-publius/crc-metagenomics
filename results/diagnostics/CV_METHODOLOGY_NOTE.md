# CV methodology comparison

Pooled 5-fold CV inflates pooled AUC by +0.050 relative to country-aware LODO (0.831 vs 0.781; DeLong p<0.001); standard LODO inflates by +0.036 (0.818 vs 0.781; DeLong p<0.001). Country-aware LODO is the honest estimator.

## Details

- **Pooled stratified 5-fold CV** (naive baseline): mean per-fold AUC 0.833, pooled AUC 0.831. Pooled-AUC gap vs country-aware LODO: +0.050 (p<0.001).
- **Standard LODO** (no country awareness): mean per-fold AUC 0.832, pooled AUC 0.818. Pooled-AUC gap: +0.036 (p<0.001).
- **Country-aware LODO** (reference; matches `train_baseline.py`): mean per-fold AUC 0.807, pooled AUC 0.781.
- **GroupKFold by cohort** (one fold per cohort, no country masking): mean per-fold AUC 0.832, pooled AUC 0.818. Pooled-AUC gap: +0.036 (p<0.001).

Pooled k-fold CV mixes samples from the same cohort across train and
test, which lets the model memorise cohort-specific batch effects
instead of generalisable CRC signal. Standard LODO removes the test
cohort but still allows another cohort from the same country to leak
population-level signal into training. Country-aware LODO is the only
protocol here that forces the model to generalise across both cohort
and country boundaries, and it is therefore the AUC we should report
and compare against external validation cohorts.

Generated by `scripts/diagnostics/cv_methodology_comparison.py`.
